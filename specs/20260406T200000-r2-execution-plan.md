# R2 Blob Storage — Execution Plan

**Date**: 2026-04-06
**Status**: Draft

## Task

Execute two specs in sequence as waves of changes, committing after each wave:

1. `specs/20260406T180000-r2-blob-storage.md` — v1 R2 upload/read/delete (Phases 1–3)
2. `specs/20260406T190000-asset-storage-billing.md` — Postgres table + Autumn billing (Phases 1–6)

Read both specs fully before starting. The handoff prompt at `specs/20260406T180000-r2-blob-storage-handoff.md` has code snippets and exact patterns for v1.

## Wave Plan

```
┌─────────────────────────────────────────────────────────────────┐
│  SPEC 1: R2 Blob Storage v1                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Wave 1: R2 Binding + Constants                                  │
│  ─────────────────────────────                                   │
│  Parallel:                                                       │
│    1.1  Add r2_buckets to wrangler.jsonc                         │
│    1.2  Add MAX_ASSET_BYTES to constants.ts                      │
│    1.3  Add FEATURE_IDS.storageBytes to billing-plans.ts         │
│  Then: bunx wrangler types (regenerate worker-configuration)     │
│  Verify: typecheck                                               │
│  Commit: "feat(api): add R2 bucket binding for asset storage"    │
│                                                                  │
│  Wave 2: Asset Routes (upload + read + delete)                   │
│  ─────────────────────────────────────────────                   │
│  Sequential:                                                     │
│    2.1  Create asset-routes.ts with all 3 endpoints              │
│    2.2  Mount in app.ts with authGuard + plan middleware          │
│  Verify: typecheck                                               │
│  Commit: "feat(api): add asset upload, read, and delete routes"  │
│                                                                  │
│  ── SPEC 1 COMPLETE ──                                           │
│  Update spec status to Implemented, add Review section           │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│  SPEC 2: Asset Storage Billing                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Wave 3: Postgres Asset Table                                    │
│  ────────────────────────────                                    │
│  Sequential:                                                     │
│    3.1  Add asset table + relations to db/schema.ts              │
│    3.2  Wire Postgres into upload handler (db.insert on upload)  │
│    3.3  Wire Postgres into delete handler (db.select + delete)   │
│    3.4  Drop customMetadata writes from upload (httpMetadata     │
│         stays, customMetadata dropped)                           │
│  Verify: typecheck                                               │
│  Commit: "feat(api): add Postgres asset index, drop              │
│           customMetadata"                                        │
│                                                                  │
│  Wave 4: Autumn Storage Feature                                  │
│  ──────────────────────────────                                  │
│  Sequential:                                                     │
│    4.1  Add storageBytes feature to autumn.config.ts             │
│    4.2  Add storage items to all plans (monthly + annual)        │
│    4.3  Replace planId gate with autumn.check() on upload        │
│    4.4  Add autumn.track() to upload + delete (via afterResponse)│
│  Then: atmn push                                                 │
│  Verify: typecheck                                               │
│  Commit: "feat(api): add Autumn storage billing with per-plan    │
│           allowances"                                            │
│                                                                  │
│  Wave 5: Listing + Usage Endpoints                               │
│  ─────────────────────────────────                               │
│  Parallel:                                                       │
│    5.1  Add GET /api/assets (list user's assets, paginated)      │
│    5.2  Add GET /api/assets/usage (user's total storage bytes)   │
│  Verify: typecheck                                               │
│  Commit: "feat(api): add asset listing and storage usage         │
│           endpoints"                                             │
│                                                                  │
│  Wave 6: Reconciliation                                          │
│  ────────────────────────                                        │
│    6.1  Add reconciliation function (Postgres SUM → autumn.usage)│
│    6.2  Wire to scheduled Worker or manual admin endpoint        │
│  Verify: typecheck                                               │
│  Commit: "feat(api): add storage billing reconciliation"         │
│                                                                  │
│  ── SPEC 2 COMPLETE ──                                           │
│  Update spec status to Implemented, add Review section           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Dependency Graph

```
Wave 1 (binding + constants)
  │
  ▼
Wave 2 (asset routes) ── SPEC 1 DONE
  │
  ▼
Wave 3 (Postgres table + wire into handlers)
  │
  ▼
Wave 4 (Autumn feature + wire into handlers)
  │
  ├──▶ Wave 5 (listing + usage endpoints) ─── independent
  │
  ▼
Wave 6 (reconciliation) ── SPEC 2 DONE
```

Wave 5 and Wave 6 are independent of each other but both depend on Wave 4. They could run in parallel, but keeping them sequential is safer since they both modify `asset-routes.ts`.

## Skills to Load

Every sub-agent should load these skills:

- `elysia` — Hono patterns (despite the name, covers Hono route handlers, error responses, middleware)
- `typescript` — code style, naming conventions
- `error-handling` — Result types, error patterns
- `monorepo` — how to run builds, tests, typecheck

Additional per-wave:
- Wave 4: `autumn` — Autumn feature/plan configuration

## Verification Between Waves

After each wave, before committing:

```bash
bunx wrangler types                   # only after Wave 1
bun run typecheck                     # after every wave
```

If typecheck fails, fix before proceeding. Don't carry broken state.

## MUST DO

- Read BOTH specs fully before starting any wave
- Read the handoff prompt (`specs/20260406T180000-r2-blob-storage-handoff.md`) for v1 code patterns
- Execute each wave as sub-agent tasks, verify, update spec, commit
- Update spec checkboxes after each wave (mark items `[x]`)
- Add implementation notes to specs when deviating from the plan
- After Spec 1 is complete, update its status to "Implemented" and add a Review section
- After Spec 2 is complete, do the same
- Stage specific files per commit — never `git add .`
- Run `atmn push` after Wave 4 to sync Autumn config

## MUST NOT DO

- Do NOT start implementing before reading both specs end to end
- Do NOT skip typecheck between waves
- Do NOT modify files outside `apps/api/`
- Do NOT install new dependencies
- Do NOT combine waves — each wave gets its own commit
- Do NOT suppress type errors with `as any` or `@ts-ignore`
- Do NOT add auth to the read endpoint (unauthenticated reads are intentional)
- Do NOT support SVG uploads
